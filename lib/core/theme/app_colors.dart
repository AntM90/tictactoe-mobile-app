import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const background = Color(0xFF060A17);
  static const surface = Color(0xFF0E1426);
  static const surfaceVariant = Color(0xFF131A2E);

  // Borders
  static const border = Color(0x1AFFFFFF);
  static const borderSubtle = Color(0x0DFFFFFF);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8B92A9);
  static const textTertiary = Color(0xFF4A5168);

  // Players
  static const playerX = Color(0xFFFF3B3B);
  static const playerO = Color(0xFF2F8FFF);
  static const playerXGlow = Color(0x33FF3B3B);
  static const playerOGlow = Color(0x332F8FFF);

  // Button
  static const buttonPrimary = Color(0xFFFFFFFF);
  static const buttonOnPrimary = Color(0xFF060A17);
}
